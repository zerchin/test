stages:
- name: build
  steps:
  - runScriptConfig:
      image: golang:latest
      shellScript: |-
        cat << EOF > hello.go
        package main
        import "fmt"
        func main() {
          fmt.Println("hello world")
        }
        go build hello.go
- name: publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: zerchin/test-pipeline:v1.0
      registry: 127.0.0.1:34826
- name: deploy
  steps:
  - applyYamlConfig:
      path: ./deployment.yaml
timeout: 60
notification: {}
